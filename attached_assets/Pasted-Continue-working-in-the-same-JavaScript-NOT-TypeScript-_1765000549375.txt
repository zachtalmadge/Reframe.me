Continue working in the same JavaScript (NOT TypeScript) + React + Tailwind CSS, mobile-first project.
The app currently shows validation error messages for various forms (multi-step main form, Step 1 backgrounds, Step 2 programs/skills, OIL steps, Step 9 resume/job posting, selection-related inputs, etc.).

Current behavior:

Error messages simply appear abruptly when validation fails (e.g., on blur, on submit, or on “Next”).

Desired behavior:

All form error messages across the entire app should fade into view smoothly when they appear (using Tailwind transitions).

This should be a consistent, global behavior for error messages, not just on a single step or field.

Important for this fix:
For this change, DO NOT perform any end-to-end (e2e) tests or automated full-flow testing.
You may do local/component-level checks only. I will manually confirm behavior in the UI.

Business requirement – Global fade-in transitions for form error messages

Update the form error handling UX so that:

All validation error messages fade in

Wherever the app displays a validation error under or near a field (e.g. <p className="text-xs text-red-600">Error text</p>):

The error should fade into view instead of appearing abruptly.

This fade-in should:

Use Tailwind’s transition-opacity and duration-XXX (e.g., 200–300ms).

Transition from opacity-0 to opacity-100 when the error becomes visible.

Apply consistently across all forms

This behavior should apply to all form-related error messages in the app, including but not limited to:

Multi-step background/offense entries (Step 1).

Rehabilitative programs and skills & strengths inputs (Step 2).

Any shared additional context fields.

O.I.L. steps (Ownership, Impact, Lessons Learned), where errors exist.

Step 9 (Reinforcing Your Qualifications, resume/job posting fields when required).

Any other validation errors shown on Selection, Disclaimer, or other small forms (if they exist).

No change to validation logic itself

When errors appear and disappear stays controlled by the existing validation (e.g., on blur, on submit, on step navigation).

Only the visual transition of the error text is changing.

Accessibility considerations

Error messages should remain:

Legible and high contrast.

Properly associated with their input (e.g., via proximity or aria-describedby if used).

The fade-in should not:

Block screen readers from reading errors when they appear (it’s a visual affordance only).

Acceptance criteria (Given / When / Then)
Scenario: Error message fades in instead of popping in

Given the user is on any form step with a required field

When they:

Leave a required field empty and trigger validation (e.g., click Next or blur),

Then:

The corresponding error message appears with a fade-in transition (opacity animation), not an abrupt jump.

The text remains fully readable once animation completes.

Scenario: No layout breaking or jitter

Given an error message appears under a field

When it fades in

Then:

The layout behaves predictably (content shifts only as much as needed to show the error).

There is no flicker, double-render, or jumpy behavior beyond normal space usage.

Scenario: Behavior is consistent across all forms

Given the user navigates through multiple steps (Step 1–9) and triggers various validation errors

When errors appear in different sections (backgrounds, programs, skills, O.I.L., resume/job posting, etc.)

Then:

All error messages use the same fade-in visual behavior.

There are no forms where errors still appear abruptly without transition.

Scenario: Errors still disappear correctly

Given an error message is visible for a field

When the user corrects the input and validation clears

Then:

The error disappears as it does currently (it’s acceptable if disappearance is instant or also faded out, but there must be no lingering artifact).

No console errors or React warnings are triggered by the animation logic.

What I want you (Replit AI) to do

Assess current error message implementation

Search for repeated patterns like:

className="text-xs text-red-600"

className="mt-1 text-sm text-destructive"

or whatever classes are used for error text.

Identify:

Whether there is a shared error component (e.g., <FormError />, <ErrorMessage />),

If yes, it should be the main integration point.

If not, this is an opportunity to centralize the fade behavior in a small, reusable component or utility.

Describe your understanding of the requirements

Explain in your own words:

That every validation error message in the UI should fade into view on appearance.

That the existing validation triggering logic must stay intact.

That consistency across all forms is required (no one-off behavior).

Identify key considerations to avoid disrupting existing behavior

Ensure:

You do not accidentally change when errors are triggered—just how they render.

You handle conditional rendering carefully ({error && <p>...) vs. always-render-with-visibility-flags).

Transitions do not cause React key issues or re-mount loops.

Propose a best-practice implementation

Preferably:

Create/standardize a small ErrorMessage component, e.g.:

function ErrorMessage({ children }) {
  if (!children) return null;
  return (
    <p className="mt-1 text-xs text-red-600 transition-opacity duration-200 opacity-100">
      {children}
    </p>
  );
}


Or, if you want true fade-in on mount, you can:

Use a small state flag (isVisible) set by useEffect to change opacity from 0 → 100.

Or rely on Tailwind + conditional class application (opacity-0 initially → opacity-100 when error is set).

The key is to:

Keep markup predictable.

Use Tailwind’s transition-opacity + duration-200 (or similar) on the error container.

Generate a step-by-step implementation plan

At a minimum include:

Step 1: Locate all error text patterns and/or shared error components.

Step 2: If there isn’t a shared component, create one like ErrorText or FormError that:

Accepts the error message as children or message.

Applies consistent Tailwind classes:

text-xs or text-sm

text-red-600 or text-destructive

transition-opacity duration-200

Condition to set opacity-100 when error exists, and opacity-0 when not (if needed).

Step 3: Replace inline <p> error elements with this shared component wherever feasible.

Step 4: Ensure that each error container is only rendered when an error exists (or, if always rendered, toggle classnames appropriately).

Step 5: Manually test:

Trigger errors on several different steps and fields.

Confirm fade-in is working everywhere and no visual glitches occur.

Keep the scope limited

Do not:

Change validation rules or which fields are required.

Change error text content.

Only adjust:

The rendering behavior and CSS/animation of error messages, across all forms.

Propose a skeleton framework

Example shared error component:

// ErrorMessage.jsx
import { useEffect, useState } from "react";

export function ErrorMessage({ message }) {
  const [visible, setVisible] = useState(false);

  useEffect(() => {
    if (message) {
      // allow next paint, then fade in
      const id = requestAnimationFrame(() => setVisible(true));
      return () => cancelAnimationFrame(id);
    } else {
      setVisible(false);
    }
  }, [message]);

  if (!message) return null;

  return (
    <p
      className={
        "mt-1 text-xs text-red-600 transition-opacity duration-200 " +
        (visible ? "opacity-100" : "opacity-0")
      }
    >
      {message}
    </p>
  );
}


Usage example:

<ErrorMessage message={errors.offenseType} />


Or, a simpler version that just uses conditional rendering with transition-opacity (if you don’t need the mount animation to be super precise).

Do not run e2e tests

For this prompt:

You may update shared components and all error message render sites.

You may perform manual checks or small component-level tests.

Do NOT run or modify any end-to-end (e2e) test suites; I will manually verify the behavior.
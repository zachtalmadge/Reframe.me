// Shared TypeScript types for Home page data structures

export type HowItWorksStep = {
  title: string;
  description: string;
};

export type Story = {
  role: string;
  quote: string;
};

export type BeforeAfterPair = {
  before: string;
  after: string;
};

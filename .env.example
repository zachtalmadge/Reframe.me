# Database (Local Development)
# For local dev, use a standard PostgreSQL connection string:
DATABASE_URL=postgresql://user:password@localhost:5432/reframeme

# Vercel Postgres (Production/Preview)
# These are automatically set when you add a Vercel Postgres database.
# Run `vercel env pull` to sync them locally for testing against the production database.
# POSTGRES_URL=
# POSTGRES_URL_NON_POOLING=
# POSTGRES_USER=
# POSTGRES_HOST=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=

# OpenAI API Configuration
AI_INTEGRATIONS_OPENAI_API_KEY=your_openai_api_key_here
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Session Configuration
SESSION_SECRET=your_session_secret_here_change_in_production

# Server Configuration
NODE_ENV=development
PORT=5000
